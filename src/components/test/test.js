import React from 'react';
import PropTypes from 'prop-types';

export defaul class test extends Component {
	constructor (props) {
		super(props) 
		// somecode in nvim
		// for sure need to tell what this so easy
	}
} 
